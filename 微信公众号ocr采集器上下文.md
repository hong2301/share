# 微信公众号OCR采集器 · 项目上下文

## 项目概述

通过**鼠标点击 + OCR 识别**自动操作微信 PC 版（4.1.11.24），采集公众号历史文章并保存为 HTML（含图片）。不依赖外部 tool.exe，纯自动化操作微信界面。

## 技术栈

- **Python 3.11** + tkinter（GUI，纯标准库界面）
- **rapidocr_onnxruntime 1.4.x**（OCR，pip 库，自带 onnx 模型）
- **requests**（抓取文章 HTML）
- **Pillow**（截图）
- **PyInstaller**（打包 exe，100MB 自包含）

## 目录结构

```
微信公众号ocr采集器/
├── main.py              # 主程序（所有逻辑）
├── README.md            # 使用文档
├── requirements.txt     # 源码运行依赖
├── 微信公众号OCR采集器.spec # PyInstaller 打包配置
├── config/              # 配置
│   ├── input.csv        # 任务列表（索引,url,公众号名称,状态）
│   ├── points.csv       # 屏幕点位（id,点位名称,x,y）
│   ├── collected.csv    # 采集记录（公众号,日期,标题,链接，去重）
│   └── ui_state.json    # 界面记忆（运行时生成）
└── 下载/                # 采集结果（按公众号分目录，git 忽略）
```

## 核心流程

### 单任务采集（7 步定位）
1. 聚焦微信窗口靠左半屏
2. 点位1(搜索栏) → 输入1 → 删除 → 点位2(搜索网络结果)
3. Ctrl+Shift+W
4. 点位1 → 输入1 → 删除 → 点位2（触发新窗口）
5. 新窗口左半屏聚焦
6. 点位3(搜索按钮) → 剪贴板粘贴任务链接 → 回车 → 等5秒
7. 点位4(作者名称) → 等5秒进入文章列表页

### 文章列表 OCR 循环
- OCR 识别点位5/7 区域的卡片（命中时间格式即卡片）
- 卡片去重：y 差值 < 卡片高度 → 同一张
- 每卡片：点击 → 5秒 → 复制链接(点位8+OCR找复制链接，3次重试，清空剪贴板) → fetch_article 抓取 → 保存 HTML
- 滚动（scroll_px）→ 再 OCR 循环
- 停止：无卡片(error) / 达到最大数量 / 时间范围结束

### 自定义时间范围模式（V1.1.0）
- 解析相对时间（昨天/今天/星期X/X月X日 → 绝对日期，按当前时间推断）
- 范围内点位点击、范围外跳过
- 置顶文章识别（第一页前3个点位出现旧→新回升）
- 停止：找到开始点后某页无范围内点位

## 时间识别规则

`resolve_article_date(text)` 支持：
- 今天/昨天/前天、X天前
- 星期X/周X/礼拜X（最近的该星期几）
- YYYY-MM-DD（绝对）
- X月X日 / MM-DD（默认今年，晚于今天推断为去年）

## 错误处理

任何一步失败 → input.csv 标记 `error:原因` → 下一个任务：
- 缺点位 / OCR 失败 / 无卡片 / 复制链接失败(3次) / 抓取失败

## 发布版

- release/ 文件夹：exe + config + README + zip，可直接分发
- exe 自包含 OCR 模型，目标电脑无需安装 Python/依赖
- 版本历史：v1.0.0 → v1.0.1 → v1.0.3 → v1.1.0（自定义时间范围）

## 运行

```bash
# 源码（需装依赖）
pip install -r requirements.txt
python main.py

# 或直接用 release/微信公众号OCR采集器.exe（免安装）
```

## 快捷键

- 回车：开始/停止采集
- ESC：立即停止（仅采集中，已拦截不传给微信）
